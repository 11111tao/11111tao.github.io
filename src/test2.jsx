// 新的测试文件
// 复制自index.jsx，但尝试解决语法错误

document.addEventListener('DOMContentLoaded', () => {
  // 这里放置所有代码
  console.log('测试文件加载成功！');

  // 模拟一些功能
  function loadBlogData() {}
  function renderBlogList() {}
  function createBlogModal() {}
  function setupBlogUpload() {}

  loadBlogData();
  renderBlogList();
  createBlogModal();
  setupBlogUpload();
});